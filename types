# 数据结构是最重要的

## 基本数据类型
内存中（物理性质），逻辑性质，应用
*** 原子类型 atom

*** 数组（物理上的数组，逻辑上的数组[链表]）
内中元素类型相同
****** 对于物理上的数组
内存中连续单元，所以能够在常量时间内访问任意元素。random access
但大小有限制，不能动态修改大小。插入与删除操作费时。

****** 逻辑上的数组之 链表
递归定义（ListCons ListHead ListTail [cons car cdr]）

*** 记录（C语言struct）——算法引论 p60
内存中连续存储


Lisp语言
*** list-of-list（表中的表）
环球网（World Wide Web）已成为国际互联网——一个全球性的计算机网络——中最有意义的部分了。
简单说来，环球网是网页的集合。每个网页都是文字、图像、电影、音频信息以及许多其他东西的序列。
更重要的是，网页还可以包含到其他网页的链接。
人们使用网络浏览器观察网页。浏览器把网页表示为文字、图像等的序列。网页中的某些文字下方会
有下划线，单击带下划线的文字可以打开一个新的网页。许多高级浏览器还提供网页编辑器，以帮助人们
创建网页。简而言之，我们应当能够在计算机上表示网页，而且应该有许多处理网页的函数。
                                        ——《h2d programs》
Web-page（网页，简称 WP）是下列三者之一
1． empty；
2． (cons s wp) ，
其中 s 是符号，wp 是网页；
3． (cons ewp wp)，
其中 ewp 和 wp 都是网页。





